#services:
#  - type: web
#    name: buildcostgh
#    env: python
#    buildCommand: "uv sync --stream && uv cache prune --ci"
#    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
#    envVars:
#      - key: DATABASE_URL
#        sync: false
#      - key: SECRET_KEY
#        sync: false
#    postDeployCommand: "alembic upgrade head"
